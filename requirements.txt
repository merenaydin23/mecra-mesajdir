# ==========================================================
# Mecra Mesajdır — Python Bağımlılıkları
# ==========================================================
# pip install -r requirements.txt

# --- Web API (server.py) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5,<3.0
python-dotenv>=1.0

# --- LLM Entegrasyonu (llm_transformer_service.py) ---
openai>=1.12
httpx>=0.26

# --- Analiz Motoru: Anlamsal Benzerlik / Bilgi Kaybı / Bozulma Zinciri ---
numpy>=1.24
torch>=2.1
transformers>=4.38
sentence-transformers>=2.5
scikit-learn>=1.3

# --- Analiz Motoru: CTA (Türkçe Morfoloji) ---
# NOT: Deployment / Build asamasinda asagidaki komut calistirilmalidir:
# python -m spacy download xx_ent_wiki_sm
spacy>=3.7
spacy-stanza>=1.0.4
stanza>=1.6.0

# --- Analiz Motoru: Duygu Yoğunluğu ---
emoji>=2.10

# --- MSSQL Veritabanı Entegrasyonu (run.py, setup.py) ---
pyodbc>=5.0

# NOT: spaCy'nin çok dilli NER modeli (xx_ent_wiki_sm) ve Stanza'nın Türkçe
# modeli ilk çalıştırmada otomatik indirilir (internet bağlantısı gerekir).
